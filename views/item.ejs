<html>

<body>
    <h1>Items</h1>
    <ul>
        <% items.map((item) => { %>
            <li>
                <h2><%= item.name %></h2>
                <p>Category: <%= item.category %></p>
                <p>$<%= item.price %></p>
            </li>
        <% }) %>
    </ul>

    <button onclick="location.href='/item/new';">Add Item</button>
</body>

</html>